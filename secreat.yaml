apiVersion: v1
kind: Secret
metadata:
  name: mysecret
  namespace: my-notes-app
type: Opaque # Opaque is the default type for arbitrary user data
data:
  # Echo the values and encode them:
  # echo -n 'admin' | base64
  # echo -n '1f2d1e2e67df' | base64
  DB_USERNAME: YWRtaW4= 
  DB_PASSWORD: MWYyZDFlMmU2N2Rm